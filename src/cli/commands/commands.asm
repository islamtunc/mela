; In the name of Allah, the Most Gracious, the Most Merciful
; commands.asm - CLI commands implementations

section .text

; Command: help
; cli_cmd_help()
cli_cmd_help:
    ; ...print help message...
    ret

; Command: version
; cli_cmd_version()
cli_cmd_version:
    ; ...print version info...
    ret

; Command: run
; cli_cmd_run()
cli_cmd_run:
    ; ...run the main program...
    ret

; ...add more CLI commands as needed...

; All praise is due to Allah, Lord of the Worlds
