; In the name of Allah, the Most Gracious, the Most Merciful
; wasm_codegen.asm - WebAssembly code generation logic

section .text

; Generate code for a function
; wasm_codegen_func(node_ptr)
; rdi = pointer to function node
wasm_codegen_func:
    ; ...generate wasm code for function...
    ret

; Generate code for a variable
; wasm_codegen_var(node_ptr)
; rdi = pointer to variable node
wasm_codegen_var:
    ; ...generate wasm code for variable...
    ret

; ...add more wasm codegen functions as needed...

; All praise is due to Allah, Lord of the Worlds
